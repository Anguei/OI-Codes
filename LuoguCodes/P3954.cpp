#include <cstdio>
#include <iostream>
int main() {
	//freopen("score.in", "r", stdin);
	//freopen("score.out", "w", stdout);
	int a, b, c;
	std::cin >> a >> b >> c;
	std::cout << a * 0.2 + b * 0.3 + c * 0.5 << std::endl;
	return 0;
}
